{
  "name": "job-match-analysis",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "oxlint apps/web/src apps/api/src",
    "lint:fix": "oxlint --fix apps/web/src apps/api/src",
    "precommit": "pnpm format && pnpm lint:fix && pnpm lint && pnpm --filter api typecheck && pnpm --filter api build && pnpm --filter web build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.24.0",
      "onFail": "download"
    }
  },
  "packageManager": "pnpm@11.24.0",
  "type": "module",
  "devDependencies": {
    "oxlint": "^1.83.0",
    "prettier": "^3.9.8"
  }
}
